# basic eks terraform

```bash
terraform init
terraform plan
terraform apply
```

### AWS command to get update KUBECONFIG file

```bash
aws eks update-kubeconfig --region ap-south-1 --name dev-eks
```